package org.example;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.List;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.apache.torque.Column;
import org.apache.torque.TorqueException;
import org.apache.torque.criteria.Criteria;
import org.apache.torque.om.mapper.RecordMapper;


/**
 * Maps ResultSet rows into Libro objects.
 *
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [Thu May 15 23:45:33 CEST 2025]
 *
 */
@SuppressWarnings("unused")
public class BaseLibroRecordMapper implements RecordMapper<Libro>
{
    /** Serial version */
    private static final long serialVersionUID = 1747345533453L;

    /** The class log. */
    private static Log log
            = LogFactory.getLog(BaseLibroRecordMapper.class);

    /**
     * Constructs the object from the current row in the resultSet.
     *
     * @param resultSet the resultSet to operate on, already pointing
     *        to the correct row. Not null.
     * @param offset a possible offset in the columns to be considered
     *        (if previous columns contain other objects),
     *        or 0 for no offset.
     * @param criteria The criteria which created the result set.
     *        If set, the attributes to set in the data object
     *        are determined from the select columns in the criteria;
     *        if no matching column can be found, null is returned.
     *        If not set, all of the table's columns are read from the
     *        result set in the order defined in the table definition.
     *
     * @return the mapped object, not null.
     *
     * @throws TorqueException when reading fields from the RecordSet fails
     *         or if a Criteria is passed which contains select columns other
     *         than the columns in the libro table.
     */
    public Libro processRow(
                ResultSet resultSet,
                int offset,
                Criteria criteria)
            throws TorqueException
    {
        Libro libro = new Libro();

        try 
        {
            libro.setLoading(true);
            if (criteria == null)
            {
                libro.setCodice(
                        getCodice(resultSet, offset + 1));
                libro.setNome(
                        getNome(resultSet, offset + 2));
                libro.setAutoreId(
                        getAutoreId(resultSet, offset + 3));
                libro.setGenereName(
                        getGenereName(resultSet, offset + 4));
                libro.setValutazione(
                        getValutazione(resultSet, offset + 5));
                libro.setStato(
                        getStato(resultSet, offset + 6));
            }
            else
            {
                // try to get columns to be mapped
                // from criteria's select columns
                boolean columnMapped = false;
                int totalOffset = offset + 1;
                List<Column> selectColumns = criteria.getSelectColumns();
                List<Column> columnsWithoutOffset = selectColumns.subList(
                        offset, 
                        selectColumns.size());
                for (Column column : columnsWithoutOffset)
                {
                    if (BaseLibroPeer.CODICE.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        libro.setCodice(
                            getCodice(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseLibroPeer.NOME.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        libro.setNome(
                            getNome(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseLibroPeer.AUTORE.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        libro.setAutoreId(
                            getAutoreId(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseLibroPeer.GENERE.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        libro.setGenereName(
                            getGenereName(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseLibroPeer.VALUTAZIONE.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        libro.setValutazione(
                            getValutazione(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseLibroPeer.STATO.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        libro.setStato(
                            getStato(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    totalOffset++;
                }
                if (!columnMapped)
                {
                    log.debug("no columns to map found in criteria, "
                        + "returning null");
                    return null;
                }
            }
            libro.setNew(false);
            libro.setModified(false);
        }
        finally
        {
            libro.setLoading(false);
        }
        return libro;
    }

    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field codice in Libro.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected Integer getCodice(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            Integer value = resultSet.getInt(columnIndex);
            if (resultSet.wasNull())
            {
                value = null;
            }
            return value;
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field nome in Libro.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getNome(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field autoreId in Libro.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected Integer getAutoreId(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            Integer value = resultSet.getInt(columnIndex);
            if (resultSet.wasNull())
            {
                value = null;
            }
            return value;
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field genereName in Libro.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getGenereName(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field valutazione in Libro.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected Integer getValutazione(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            Integer value = resultSet.getInt(columnIndex);
            if (resultSet.wasNull())
            {
                value = null;
            }
            return value;
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field stato in Libro.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getStato(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }

}
